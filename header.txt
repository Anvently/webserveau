

MANDATORY TO CHECK:
Connection if "close" we should close the connection after sending response
Content-Encoding:identity =>if other response = 415 (when receiving a body)
Content-Length
Content-Type example "Content-Type: text/html" => use MIME, ";" might be present
Expect: 100-continue => parse headers and returns either 100-continue OR 417 if for example body size is too large
Host: must be present at least empty (?) else => 400
TE: not sure
Trailer => if chunked
Transfer-Encoding: if != chunked => 501 + close


MAYBE?:
-Accept
-Accept-Encoding ? => if identity; q=0 renvoyer erreur 406 ?
Accept-Ranges => le client est sense verifier que le serveur accepte ce genre de requete avt de faire
	une demande effective. On peut tjrs repondre Accept-Ranges:none il semblerait.
	related: Content-Range


=> PARSING body
	- if not chunked
		=> content-length VS max body size


HEADER TO ADD ALWALYS TO A RESPONSE:

Allow => "Allow: GET, #all allowed method"
Content-Length OR Transer-Encoding: chunked
Connection: close or keep-alive
Content-Type => USE MIME || generated by the CGI
Date => "Date: Tue, 23 May 2024 19:21:44 GMT

fichier statique
	- ifstream
		- 
CGI
	- pipe => outBuffers
headears/ERROR
	- strings => outBUffers



