#include <iostream>
#include <string>

class CGIProcess {

	private:

	public:

		/// @brief
		/// @return ```0``` if not finished, ```> 0``` if finished, ```< 0```
		/// if error.
		int	checkEnd();

};